		void max (int a, int b)
			
		  {
			if (a > b)
			{Console.WriteLine(a);}
		
			if(a < b)
			{Console.WriteLine(b);}
		
	    	}
		
		 int numA = 5;
		 int numB = 7;
		 max(numA, numB);
				
		 int numC = 2;
		 int numD = 10;
		 max(numC, numD);
		
		 int numN = -9;
		 int numM = -3;
		 max(numN, numM);
